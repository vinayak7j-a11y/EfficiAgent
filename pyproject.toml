# Project: EfficiAgent

[project]
name = "efficiagent"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["flask==3.0.3"]

[tool.ruff]
line-length = 100
exclude = ["part1_token_optimization/*", "part2_debugging/*"]

[tool.ruff.lint]
select = ["E", "F", "W"]
